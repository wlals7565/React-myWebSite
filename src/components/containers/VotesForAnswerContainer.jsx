import React, { useState } from 'react'

const VotesForAnswerContainer = ({initialState}) => {
  const [votes, setVotes] = useState(initialState)
  return (
    <div>VotesForAnswerContainer</div>
  )
}

export default VotesForAnswerContainer